---
description: "Full form_* MCP namespace: Typeform-class multi-step forms, lead capture, NPS / CSAT surveys, intake forms. Reuses booking_flows table with kind='form'. 15+ field types, conditional logic with variables + hidden fields, structural validation, embed snippet generation via the embed.spideriq.ai/v1/loader.js loader (3 KB, inline + popup). Standalone route /f/{flow_id} (legacy /book/{flow_id} 301-redirects here). NOT in @spideriq/mcp-publish — only in kitchen-sink @spideriq/mcp@^1.20.1. Use whenever the user asks for 'a form', 'survey', 'questionnaire', 'NPS', 'lead capture', 'intake form', 'Typeform alternative'."
alwaysApply: false
---
# forms

Forms — Typeform-class multi-step forms (lead capture, NPS / CSAT surveys, intake forms, signup wizards) for tenant sites. Built on the same `booking_flows` engine that powers SpiderBook, discriminated by `kind='form'`.

**Status: developer / agent preview (Wave 2).** The dashboard editor for business users (P1.M1) is in active development; for now, forms are authored end-to-end via CLI / MCP / VSCode extension.

**Exposed via:** `@spideriq/mcp@^1.20.1` (kitchen-sink) — **20+ `form_*` tools + `booking_flow_list` with `?kind=` filter**. **NOT in `@spideriq/mcp-publish`** — see [the caveat below](#mcp-package-caveat).

> **Premium design options** — start here when you want a polished look, not the default look: [shared/guides/forms-design-guide.md](../../guides/forms-design-guide.md). Covers theme presets, layout variants, field-level styling, welcome / thank-you screens, progress indicators, transitions, brand assets, accessibility, and the 14-rule validator. Use **after** Phase 0 (MCP bundle check) but **before** publish.

## What you ship with this

- A multi-step form widget (15+ field types: short_text, long_text, email, phone, number, dropdown, checkbox, picture_choice, rating, nps, opinion_scale, date, file_upload, statement, yes_no)
- A standalone route at `/f/{flow_id}` on the tenant's primary domain. (Legacy `/book/{flow_id}` 301-redirects to `/f/{flow_id}` for back-compat.)
- An embed loader at `https://embed.spideriq.ai/v1/loader.js` (3 KB gzip) — drops a form into any third-party page in one `<script>` tag, two declarative shapes (inline + popup)
- Conditional logic: rules with variables, hidden fields (URL-param capture for utm_*, etc), client-side structural validation
- Copy-paste embed snippets generated by `form_get_embed_snippet`
- A test-submit path (`?test=true`) for QA without polluting real lead data

## When to use

- A tenant needs a multi-step form: lead capture, signup wizard, intake form, NPS / CSAT survey
- You're migrating a Typeform form to SpiderPublish (cost reduction; agent-driven authoring)
- You need to embed a form on an external site (Webflow, Shopify, WordPress, Framer, plain HTML)
- You want hidden-field capture for tracking parameters (utm_source, ref, ...)
- The form needs branching logic (e.g. "if user picks 'Yes', show fields A-C; else show D-E")

## When NOT to use

- Single-question subscribe form / newsletter signup → use a page with a `form` block (lighter)
- Calendar / appointment slot picking → use [booking](../booking/) — that's what cal.com event-types are for
- Long-form content authoring (blog posts, docs) → use [content-platform](../content-platform/)
- E-commerce checkout → no inventory / payment model in Forms

## MCP package caveat

The `form_*` tools live in **`@spideriq/mcp@^1.20.1`** (the kitchen-sink package) — **not** in `@spideriq/mcp-publish@^1.17.2` (the atomic publish package, the starter-kit default).

This is deliberate: Antigravity, Claude Desktop, and Codex-on-Responses silently drop MCP servers that report more than 128 tools. `mcp-publish` stays under the ceiling so it loads cleanly across every IDE; the kitchen-sink `mcp` package opts into the higher tool count.

**If you're using the starter kit's default `.mcp.json` (which points at `mcp-publish`), the `form_*` tools won't appear.** Three options:

1. **Switch the existing entry:** edit `.mcp.json` and replace `@spideriq/mcp-publish` with `@spideriq/mcp@^1.20.1`. You get every tool (publish + booking + forms + mail + leads + gate + admin) at the cost of the higher tool count.
2. **Add a second MCP server:** keep `mcp-publish` as the default, add `mcp` as `spideriq-forms` (or similar) and only enable it in form-authoring sessions.
3. **Wait for `@spideriq/mcp-forms`:** an atomic forms-only package is on the follow-up list — it'll let you pull just the forms surface without the kitchen-sink overhead.

A future starter-kit release will surface this choice in `.mcp.json` directly.

## Phase 0 — Verify your MCP bundle before authoring

Before any `form_*` call, confirm the bundle exposes them. A stale bundle is the #1 cause of confidently-wrong work — see the [Phase 0 ritual in AGENTS.md](../../content/agents-catalog.md) for the full ≤30-second check. Quick form-specific probe:

```
form_preview_url({ flow_id: "<any existing flow_id>" })
# Expected response.public_url contains "/f/<flow_id>" — NOT "/book/<flow_id>".
# If the URL still says /book/, your bundle is stale. STOP and update before authoring.
```

## End-to-end: from zero to an embedded form

```bash
# 1. List existing flows (to avoid name collisions)
spideriq form list --kind form           # via the new ?kind= filter
# OR via MCP: booking_flow_list({ kind: "form" })

# 2. Create a draft form with two initial fields
spideriq form create \
  --name "Free trial signup" \
  --business <business_uuid> \
  --field 'fields=[{"id":"q1","type":"email","label":"Your work email","required":true},{"id":"q2","type":"short_text","label":"Company name","required":true}]'
# → returns flow_id

# 3. Add a third field via MCP
form_add_field({
  flow_id: "<flow_id>",
  field: { id: "q3", type: "dropdown", label: "Team size", options: [
    { label: "1-10",   value: "small"  },
    { label: "11-50",  value: "medium" },
    { label: "51+",    value: "large"  }
  ]}
})

# 4. Add conditional logic: if team_size = "large", jump to enterprise field
form_add_logic_rule({
  flow_id: "<flow_id>",
  rule: {
    when: { field_id: "q3", op: "equals", value: "large" },
    then: { action: "jump_to", field_id: "q_enterprise" }
  }
})

# 5. Validate the whole flow (client-side; no API call)
form_validate({ flow: <full flow blob> })
# → { errors: [], warnings: [] }

# 6. Test-submit (with ?test=true — no real lead row created)
form_test_submit({
  flow_id: "<flow_id>",
  answers: { q1: "test@example.com", q2: "Acme Corp", q3: "small" }
})

# 7. Publish (2-phase confirm)
spideriq form publish <flow_id>
# → preview + confirm_token
spideriq form publish <flow_id> --confirm <token>
# → status: active

# 8. Get the embed snippet (local-only — no API call)
spideriq form embed-snippet <flow_id> --mode popup --button-text "Get Free Trial"
# → <script async src="https://embed.spideriq.ai/v1/loader.js"></script>
#   <button data-spiderflow-flow="<flow_id>" data-spiderflow-mode="popup"
#           data-spiderflow-trigger-text="Get Free Trial">Get Free Trial</button>

# 9. Paste into any third-party page (Webflow / Shopify / WordPress / plain HTML)

# 10. Visual proof — confirm the embed actually rendered (Wave 3 contract):
content_visual_check({
  page_url: "https://acme.com/landing-with-form",
  viewport: "desktop",
  timeout_ms: 30000
})
# Assert: response.dom.shadow_hosts includes "spideriq-form".
# (The iframe body_text_preview is OPAQUE to the parent page — never assert on it.)
```

## Templates — starter forms, ready to clone

Five globally-seeded form templates ship with the platform. Clone one and tailor it instead of starting blank.

| Slug | Category | Purpose |
|---|---|---|
| `contact-form` | `contact` | Name + email + subject + message |
| `lead-gen-quiz` | `lead_gen` | Qualifying multi-step quiz with branching |
| `nps-survey` | `survey` | Single NPS question + open-ended follow-up |
| `event-rsvp` | `event_rsvp` | RSVP with party-size + dietary requirements |
| `job-application` | `application` | Role + resume upload + cover letter |

Discovery + clone (one round-trip each):

```
# List globally-seeded form templates (cookie auth or PAT):
GET /api/v1/dashboard/booking/templates/global?kind=form&format=yaml

# Clone into the current tenant as a draft:
POST /api/v1/dashboard/booking/templates/clone
{ "template_id": "<template_id>", "new_name": "Acme contact form" }
# → { flow_id, kind: "form", status: "draft" }
```

The MCP-equivalent tool is exposed under the booking namespace (templates back both `kind='form'` and `kind='booking'`). Verify the live name against your bundle's `tools/list` — `booking_template_list` and `booking_template_clone` are the current shapes; older bundles may still expose `flow_template_*`.

More categories (`feedback`, `intake`, `signup`) ship in the next Wave 3.5 chore.

## Backend caveats (current state)

These will be resolved in P1.M1 / P1.M2; documented inline so agents work around them gracefully:

| Issue | Workaround |
|---|---|
| `form_create` requires `business_id` (FK constraint) | Pass any business UUID owned by the brand. Backend will make this optional for `kind='form'`. |
| `form_publish` requires Cal.com `title` / `length_minutes` / `team_id` | Pass any non-empty title, `length_minutes=1`, `team_id=0`. Backend will go kind-aware. |
| `form_test_submit ?test=true` honoured only for `LOAD_TEST_CLIENT_ID` | Until backend extends honour, use a real flow with a unique answer payload + clean up the test row manually. |
| Server-side `duplicate` and `validate` endpoints don't exist | Both work — `form_duplicate` composes client-side as `get + create`; `form_validate` runs entirely client-side against the locked schema. |

## VSCode extension (SpiderPublish 0.4.0+)

If you're using the [SpiderPublish VSCode / Cursor / Antigravity extension](https://github.com/martinshein/SpideriQ-ai/tree/main/SpiderPublish), forms are first-class:

- **Pull:** `SpiderPublish: Pull Content` enumerates every `kind='form'` row and writes each to `./forms/<flow_id>.json`
- **Edit:** open any `./forms/*.json`; inline form validator catches 14 rule classes (R0–R14) with red squiggles
- **Push:** `SpiderPublish: Push Content` includes form rows alongside pages / posts / components
- **Preview:** `SpiderPublish: Preview Form` opens `/f/<flow_id>` in your default browser
- **Visual-check panel (0.4.0+):** runs the Playwright sidecar in-IDE; surfaces `dom.shadow_hosts`, console errors, failed requests, and screenshot diff in a side panel

## Embed loader API

The loader (`embed.spideriq.ai/v1/loader.js`) exposes `window.SpiderFlow` (the public JS API name is preserved — only product/user-facing naming changed):

- `window.SpiderFlow.version` — running loader version (self-reported from package.json)
- `window.SpiderFlow.create(element, options)` — programmatic mount
- `window.SpiderFlow.scan()` — re-scan the DOM for `[data-spiderflow-flow]` (call after SPA route changes)
- `window.SpiderFlow.destroyAll()` — tear down every mounted iframe

Configuration via `data-*` attributes (names preserved for back-compat with existing embeds):
- `data-spiderflow-flow="<flow_id>"` (required)
- `data-spiderflow-mode="inline|popup"` (default: inline)
- `data-spiderflow-domain="https://forms.spideriq.ai"` (override the form-render domain)
- `data-spiderflow-trigger-text="Open Form"` (popup only)
- `data-spiderflow-on-complete="window.myCallback"` (dotted-path lookup; no `eval`)
- `data-spiderflow-auto-open="true"` (popup only — open immediately on mount)
- `data-spiderflow-hide-headers="true"` (suppress form chrome for a tighter embed)
- `data-prefill-<key>="<value>"` (any number; populates hidden fields by key)

Two embed shapes:

```html
<!-- Inline: replace a div with an iframe -->
<div data-spiderflow-flow="<flow_id>" data-spiderflow-mode="inline"></div>
<script async src="https://embed.spideriq.ai/v1/loader.js"></script>

<!-- Popup: button → modal iframe (lazy-loaded; iframe doesn't fetch until click) -->
<button data-spiderflow-flow="<flow_id>" data-spiderflow-mode="popup"
        data-spiderflow-trigger-text="Open Form">Open Form</button>
<script async src="https://embed.spideriq.ai/v1/loader.js"></script>
```

Origin validation is built in — `event.source === iframe.contentWindow && event.origin === configured_domain` before any side-effect. Two embeds on one page never cross-talk (each drops messages whose `flowId` doesn't match).

## Wave 3 contracts (Agent Trust Hardening, 2026-05-20)

Every Forms-read endpoint accepts `?format=llm` and returns an OPVS `guidance:` envelope. Use it when you're not sure which tool to chain next.

```
GET /api/v1/dashboard/booking/flows/<flow_id>?format=llm
# → { ...flow, guidance: { use, not, next, warn, pitfalls, limits } }
```

`guidance` keys are FROZEN — the only six are `use / not / next / warn / pitfalls / limits`. Treat any other top-level key as documentation, not contract.

Destructive form ops (`form_delete`, `form_publish`, `form_unlock`) default to `dry_run=true` and return a `confirm_token`. Call again with `confirm_token` to actually apply. **Honesty rule (F-8, 2026-05-20):** read-side and side-effect-free ops do NOT carry a `confirm_token` envelope. If `guidance.warn` claims something is mandatory and the default call still returned a successful resource, the warning is wrong — file an upstream issue, don't work around it.

## Reference

Full API surface: [schema.yaml](./schema.yaml) — 21 methods with parameter schemas.

Live tool catalog: `docs.spideriq.ai/mcp/forms/` (coming with Wave 2 docs publish).
